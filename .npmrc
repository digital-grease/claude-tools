# Dependency cooldown — wait 7 days before adopting a release. See ../COOLDOWNS.md.
# npm's min-release-age is a bare integer in DAYS (npm >= 11.10); "7d" is invalid
# and silently disables the cooldown on npm 11.14+.
min-release-age=7
